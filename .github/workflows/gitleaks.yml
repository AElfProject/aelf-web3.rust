name: gitleaks

on:
  push:
    branches:
      - "**"
  pull_request:

jobs:
  gitleaks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: gacts/gitleaks@v1.8.0
        with:
          config-path: .gitleaks.toml
